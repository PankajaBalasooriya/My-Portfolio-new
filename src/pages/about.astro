---
import PageLayout from '../layouts/PageLayout.astro';
import Section from '../components/Section.astro';
import Prose from '../components/Prose.astro';
import { url } from '../lib/url';
import { site, socials } from '../lib/site';

/**
 * This page is the narrative; /cv is the record. The education, experience,
 * volunteering and awards timelines live only on /cv — rendering them here as
 * well made this page a strict subset of that one, with nothing of its own.
 */
const glance = [
  { term: 'Now', detail: site.role.affiliation },
  { term: 'Studying', detail: site.role.study },
  ...(site.location ? [{ term: 'Based', detail: site.location }] : []),
  { term: 'Focus', detail: site.knowsAbout.join(' · ') },
];

const profiles = socials.filter((s) => !s.href.startsWith('mailto:'));
---

<PageLayout
  title="About"
  description="Background, research interests, and how to get in touch."
  lede="Placeholder standfirst — one or two sentences of real positioning copy will go here."
>
  <Prose>
    <p>
      Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut
      labore et dolore magna aliqua. This long-form biography is placeholder text and will be
      replaced with real copy.
    </p>
    <p>
      Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea
      commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum
      dolore eu fugiat nulla pariatur.
    </p>
    <p>
      Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit
      anim id est laborum. Sed ut perspiciatis unde omnis iste natus error sit voluptatem.
    </p>
  </Prose>

  <Section title="At a glance" class="mt-16">
    <dl class="grid gap-x-8 gap-y-4 sm:grid-cols-[8rem_1fr]">
      {
        glance.map((row) => (
          <div class="sm:contents">
            <dt class="text-sm text-faint sm:pt-0.5">{row.term}</dt>
            <dd class="mt-0.5 text-muted sm:mt-0">{row.detail}</dd>
          </div>
        ))
      }
    </dl>

    <p class="mt-8">
      <a
        href={url('/cv')}
        class="text-sm text-accent underline decoration-1 underline-offset-4 transition-colors hover:text-accent-hover"
      >
        Full record on the CV <span aria-hidden="true">→</span>
      </a>
    </p>
  </Section>

  <Section title="Get in touch" class="mt-16">
    <p class="max-w-[60ch] text-muted">
      Email is the best way to reach me. I am also on the networks below.
    </p>

    <p class="mt-6">
      <a
        href={`mailto:${site.author.email}`}
        class="inline-block rounded-md bg-accent px-4 py-2 text-sm font-medium text-bg transition-colors hover:bg-accent-hover"
      >
        {site.author.email}
      </a>
    </p>

    <ul class="mt-6 flex flex-wrap gap-x-5 gap-y-2 text-sm">
      {
        profiles.map((profile) => (
          <li>
            <a
              href={profile.href}
              rel="me noopener"
              class="text-muted underline decoration-1 underline-offset-4 transition-colors hover:text-accent"
            >
              {profile.label}
            </a>
          </li>
        ))
      }
    </ul>
  </Section>
</PageLayout>
